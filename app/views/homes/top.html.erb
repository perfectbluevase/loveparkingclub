<div class="container text-center">
  <div class="row align-items-center">
    
    <div class="col-1">
    </div>
    
    <div class="col-10 justify-content-center pb-4">
      <%= image_tag 'logo_big.jpg', size: '720x380', hspace: '10', vspace:'10' %>
      <%= form_with url: public_parkings_path, method: :get do |f| %>
      <%= f.text_field :oreno_hash_tag, placeholder: "Put one key-word." %>
      <%= f.submit '検索' %>
      <% end %>
    </div>
    
    <div class="col-1">
    </div>
    
  </div>
</div>
	

<%# 以下表示されない（仮置き）　%>
<% if false %>
  <% if params[:oreno_hash_tag].present? %>
    <%= params[:oreno_hash_tag] %>の検索結果
  <% end %>
  <% @parkings.each do |parking| %>
  <div>
    <p>駐車場名：
    <%= link_to public_parking_path(parking.id) do %>
      <%= parking.parking_name %>
    <% end %>
    </p>
    <p>住所：<%= parking.parking_address %></p>
    <p>運営会社：<%= parking.parking_operator %></p>
    <p>詳細：<%= parking.parking_fee_info %></p>
    <p>駐車場についてのコメント：<%= parking.parking_review %></p>
    <p><%= link_to "#{parking.post_comments.count} コメント", public_parking_path(parking.id) %></p>
  </div>
  <% end %>
<% end %>